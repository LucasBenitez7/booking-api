apiVersion: v1
kind: Service
metadata:
  name: booking-api
  namespace: booking
  labels:
    app: booking-api
    component: api
spec:
  type: ClusterIP
  selector:
    app: booking-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
