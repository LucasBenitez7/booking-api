# DO NOT commit real values — this file is a template only.
# In production, secrets are injected via CI/CD (GitHub Actions + kubectl create secret)
# or a secrets manager (AWS Secrets Manager, Vault, External Secrets Operator).
apiVersion: v1
kind: Secret
metadata:
  name: booking-api-secrets
  namespace: booking
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://user:password@host:5432/dbname"
  REDIS_URL: "redis://:password@booking-redis:6379/0"
  REDIS_PASSWORD: "change-me"
  SECRET_KEY: "change-me-32-chars-minimum"
  JWT_SECRET_KEY: "change-me-32-chars-minimum"
  CELERY_BROKER_URL: "redis://:password@booking-redis:6379/1"
  CELERY_RESULT_BACKEND: "redis://:password@booking-redis:6379/2"
